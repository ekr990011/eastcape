  <p>
    <strong>Comment:</strong>
    <%= comment.reply %>
  </p>
  posted by:
<% if comment.user_id.nil? %>
Anonymous
<% else %>
<%= User.find(comment.user_id).username %>
<% end %>
comment:
<%= comment.id %>
<br>
forum:
<%= @forum.id %>
<br>
<div>
  <!--<li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown" %>Edit Comment<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <%# render @forum.comments %>
          </ul>
        </li>-->
  <% if ((current_user != nil) && (current_user == comment.user))  || (current_user && current_user.admin?) %>
    <li><%= link_to 'Edit', edit_forum_comment_path(comment.forum, comment) %></li>
    <li><%= link_to 'Delete', [comment.forum, comment], method: :delete %></li>
  <% end %>
</div>